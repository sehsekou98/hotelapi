server:
  port: 9192

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lisam_hotel_db
    username: sehsekou98
    password: password1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
      file-size-threshold: 2KB

security:
  jwt:
    secret-key:
    expires-length: 3600000
